type InputModel = {
  type?: string;
  name: string;
  title: string;
  required?: boolean;
  val?: string;
  valid?: boolean;
  placeholder?: string;
  visibleTitle: boolean;
};

export default InputModel;
